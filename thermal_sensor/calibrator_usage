"""
Example Usage of the calibrator script.

Modifiable refresh rate.
"""

mlx = MLX90640(i2c)
mlx.refresh_rate = RefreshRate.REFRESH_2_HZ

frame = init_float_array(768)
while True:
    calib_frame = mlx.get_frame(frame)
    return frame 
